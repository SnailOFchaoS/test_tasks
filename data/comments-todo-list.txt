Комментарии к задаче Todo List:

использовался тип T при создании хука. Он позволяет создавать переиспользуемые типы и функции, которые работают с разными типами данных

была проблема с испоьлзованием localStorage при серверном рендеринге(почему-то use client не сработало)
нужно было, чтобы первый рендер совпадал для этого во первых использовалось const [value, setValueState] = useState<T[]>(initialValue);
на сервер и на клиенте использовалось initialValue, а после значенеи получалось и стореджа при помощи useEffect после монтирования компоненты

также проработал использование useCallback для предотвращения ререндеров. В качестве пропса он может принимать пользовательские ивернты
useCallback((e: React.MouseEvent<HTMLButtonElement>) =>

e.currentTarget.value - получение данных из инпута без использования value и state. Думаю также можно получить их через useRef
